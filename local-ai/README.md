# Mangrok local model profile

```bash
ollama pull llama3.2
ollama create mangrok-alchemy -f local-ai/ollama/Modelfile
```

Use `http://127.0.0.1:11434/v1` and model `mangrok-alchemy` in Alchemy Lab. The repository stores configuration and prompts, not model weights.
