import test from "node:test";import assert from "node:assert/strict";import { readFile } from "node:fs/promises";import path from "node:path";import { fileURLToPath } from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");
const read=p=>readFile(path.join(root,p),"utf8");
test("browser bundle contains no service-role environment access",async()=>{const texts=await Promise.all(["runtime-config.js","src/app.js","src/cloud.js","src/store.js"].map(read));for(const text of texts)assert.ok(!text.includes("SUPABASE_SERVICE_ROLE_KEY"));});
test("database contract enables RLS and contributor field guards",async()=>{const sql=await read("supabase/migrations/001_platform.sql");for(const phrase of ["alter table public.recipes enable row level security","guard_and_version_recipe","Contributors cannot change ownership, privacy, or sealed-note fields","recipe_assets_storage_select","get_shared_recipe"])assert.ok(sql.includes(phrase),phrase);});
test("legacy automation is review-only",async()=>{const code=await read("supabase/functions/legacy-review/index.ts");assert.ok(code.includes('status: "review_pending"'));assert.ok(code.includes("released: 0"));assert.ok(code.includes("does not contact recipients"));});
test("print adapter is authenticated and idempotent",async()=>{const code=await read("supabase/functions/print-order/index.ts");assert.ok(code.includes("auth.getUser"));assert.ok(code.includes('eq("request_id", requestId)'));assert.ok(code.includes('"idempotency-key": requestId'));assert.ok(code.includes("proof_required"));assert.ok(code.includes("provider_not_configured"));});
