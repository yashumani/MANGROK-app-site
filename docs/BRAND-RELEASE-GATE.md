# Mangrok brand release gate

This branch is materialized by the trusted repository workflow before merge. The corrected gate integrates the 4,301-entry ingredient catalog, bounded agent and memory source, source-aware Recipe Atelier, restored Alchemy and local LLM paths, vector identity reveal, and the 32-piece layered sticker studio. It must pass the complete `npm run check` suite before deployment.
