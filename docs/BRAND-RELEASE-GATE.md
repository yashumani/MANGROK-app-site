# Mangrok brand release gate

This branch is materialized by the repository release workflow before merge. The gate must integrate the vector identity, session-aware logo reveal, layered sticker editor, printed sticker output, restored Alchemy, source-aware Recipe Atelier, and the versioned ingredient catalog; then pass the complete `npm run check` suite.
