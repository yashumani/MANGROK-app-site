# Recipe Atelier and Alchemy recovery

This branch exists to trigger the one-time pull-request materialization gate. The gate restores the complete ingredient and bounded-agent dependency graph, integrates the source-aware Recipe Atelier, preserves the local/private LLM adapters, restores Alchemy as a primary destination, runs the complete repository check, and replaces this marker with the normal reviewable source tree.
