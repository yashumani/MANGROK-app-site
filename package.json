{
  "name": "mangrok-recipe-vault",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first, permission-aware vault for secret recipes, family stories, print books, and legacy instructions.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run test && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
