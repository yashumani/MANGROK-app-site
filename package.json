{
  "name": "mangrok-recipe-vault",
  "version": "3.5.0-alpha.6",
  "private": true,
  "type": "module",
  "description": "A local-first recipe atelier with source-aware starter recipes, restored Alchemy, deterministic culinary intelligence, local/private LLM adapters, and print-ready preservation.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run test && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
