{
  "name": "mangrok-recipe-vault",
  "version": "3.6.0-alpha.7",
  "private": true,
  "type": "module",
  "description": "A local-first recipe atelier with restored culinary Alchemy, source-aware references, global ingredient knowledge, a vector brand reveal, and a layered cookbook sticker studio.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run test && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
